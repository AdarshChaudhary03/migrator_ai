# Quarkus Migration

This project was migrated from Spring Boot to Quarkus.

## Next Steps

1.  Run `mvn compile` to verify the project builds correctly.
2.  Test the application startup with `mvn quarkus:dev`.
3.  Review the generated files for correctness.